/**
 * @owner Will
 * Create Mongo indexes from spec §3.
 */
import "dotenv/config";

async function main() {
  console.log("TODO(Will): create unique + vector indexes.");
  process.exit(0);
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});
