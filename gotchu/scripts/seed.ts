/**
 * @owner Will
 * Seed 25 fake CMU students with preferenceText (+ embeddings when wired).
 */
import "dotenv/config";

async function main() {
  console.log("TODO(Will): seed 25 users — Divya needs this for matching.");
  // Placeholder so the script path exists and is runnable
  process.exit(0);
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});
